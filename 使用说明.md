# Mimic-Red 使用说明

## 📚 目录
- [快速开始](#-快速开始)
- [命令行参数](#-命令行参数)
- [使用示例](#-使用示例)
- [存储格式说明](#-存储格式说明)
- [数据字段说明](#-数据字段说明)
- [进阶配置](#-进阶配置)
- [常见问题排查](#-常见问题排查)
- [数据转换工具](#-数据转换工具)

---

## 🚀 快速开始

### 1. 安装依赖

```bash
# 生产环境（推荐，版本锁定）
pip install -r requirements-lock.txt

# 开发环境（最小版本约束）
pip install -r requirements.txt
```

### 2. 启动 Chrome 调试模式

**Mac/Linux:**
```bash
chmod +x start_chrome.sh
./start_chrome.sh
```

**Windows:**
双击运行 `start_chrome.bat`

启动后会自动打开小红书网站，请手动扫码登录。

> ⚠️ **重要**: 登录成功后，**不要关闭这个浏览器窗口**，否则爬虫无法连接。

### 3. 运行爬虫（最简单示例）

```bash
# 默认使用 SQLite 存储，爬取20条
python mimic_red_engine.py -k "澳洲留学"

# 指定数量和格式
python mimic_red_engine.py -k "澳洲留学" -l 50 -s csv
```

---

## 📋 命令行参数

| 参数 | 简写 | 说明 | 默认值 | 示例 |
|------|------|------|--------|------|
| `--keywords` | `-k` | 关键词列表（空格分隔） | "澳洲留学" | `-k "悉尼大学" "墨尔本"` |
| `--limit` | `-l` | 每个关键词爬取数量 | 20 | `-l 50` |
| `--daily-limit` | `-d` | 每日总爬取上限 | 50 | `-d 100` |
| `--storage` | `-s` | 存储格式 | sqlite | `-s csv` |
| `--output` | `-o` | 输出目录 | datas | `-o results` |
| `--min-likes` | | 最少点赞数过滤 | 0 | `--min-likes 100` |
| `--no-warmup` | | 跳过会话预热 | False | `--no-warmup` |
| `--no-shuffle` | | 不打乱关键词顺序 | False | `--no-shuffle` |

### 存储格式选项

| 格式 | 值 | 特点 | 适用场景 |
|------|---|------|----------|
| **SQLite** | `sqlite` | 关系型数据库，支持高级查询 | 大量数据，需复杂查询 |
| **CSV** | `csv` | Excel可直接打开 | 数据分析、导入其他工具 |
| **JSON** | `json` | 结构化文本 | 程序对接、API交换 |
| **Excel** | `excel` | 多工作表，格式美观 | 报告、展示 |

---

## 💡 使用示例

### 示例 1：新手推荐配置（安全稳定）

```bash
python mimic_red_engine.py \
  -k "澳洲留学" \
  -l 20 \
  -d 100 \
  -s sqlite
```
- 每个关键词爬取20条
- 每日上限100条（防止封号）
- 使用SQLite存储（支持断点续爬）

### 示例 2：多关键词批量采集

```bash
python mimic_red_engine.py \
  -k "悉尼大学" "墨尔本大学" "昆士兰大学" "UNSW" \
  -l 30 \
  -d 150 \
  -s csv \
  --min-likes 50
```
- 4个关键词，每个30条
- 过滤点赞数<50的笔记
- 保存为CSV格式

### 示例 3：高质量内容精选

```bash
python mimic_red_engine.py \
  -k "澳洲移民政策" \
  -l 50 \
  --min-likes 500 \
  --daily-limit 200 \
  -s excel
```
- 只采集点赞数≥500的高质量笔记
- 保存为Excel方便查看

### 示例 4：长期数据收集（推荐）

```bash
# 周一至周五每天运行
python mimic_red_engine.py \
  -k "澳洲留学攻略" \
  -l 20 \
  -d 100 \
  -s sqlite

# 周六日休息，模拟真实用户行为
```

---

## 📦 存储格式说明

### 文件输出位置

根据选择的格式，数据会自动保存到对应目录：

```
项目根目录/
├── sqlite_datas/
│   └── notes.db                    # SQLite数据库（持续追加）
├── csv_datas/
│   ├── notes_20260212_140000.csv       # 笔记数据（带时间戳）
│   └── comments_20260212_140000.csv    # 评论数据
├── json_datas/
│   └── notes_20260212_140000.json      # 包含笔记+评论
└── excel_datas/
    └── notes_20260212_140000.xlsx      # 多工作表Excel
```

### SQLite 数据库结构

包含两张表：
- **notes**: 笔记主表
- **comments**: 评论表（外键关联note_id）

查看方法：
```bash
# 命令行查看
sqlite3 sqlite_datas/notes.db "SELECT * FROM notes LIMIT 5;"

# 图形工具（推荐）
# Mac: DB Browser for SQLite
# Win: SQLiteStudio
```

### CSV 文件特点

- **编码**: UTF-8-BOM（Excel直接打开不乱码）
- **分隔符**: 逗号
- **去重**: 启动时扫描所有历史CSV文件（2026年新增）

### JSON 文件结构

```json
{
  "notes": [
    {
      "note_id": "...",
      "title": "...",
      "desc": "..."
    }
  ],
  "comments": [...],
  "crawl_time": "2026-02-12T14:00:00",
  "total_notes": 100,
  "total_comments": 458
}
```

### Excel 工作表

- **Notes** 工作表：笔记数据
- **Comments** 工作表：评论数据

> ⚠️ **注意**: Excel格式需要额外安装依赖：
> ```bash
> pip install pandas openpyxl
> ```

---

## 🔧 数据字段说明

### 笔记表（Notes）

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `note_id` | 文本 | 笔记唯一ID | "65a3f2..." |
| `url` | 文本 | 笔记链接 | "https://..." |
| `title` | 文本 | 标题 | "澳洲八大申请攻略" |
| `desc` | 文本 | 正文内容 | "详细介绍..." |
| `note_type` | 文本 | 类型 | normal/video |
| `author_id` | 文本 | 作者ID | "5f2a..." |
| `author_name` | 文本 | 作者昵称 | "留学小助手" |
| `liked_count` | 整数 | 点赞数 | 1520 |
| `collected_count` | 整数 | 收藏数 | 890 |
| `comment_count` | 整数 | 评论数 | 234 |
| `total_interaction` | 整数 | 总互动量 | 2644 |
| `traffic_level` | 文本 | 流量等级 | 爆款/优质/普通/低质 |
| `tags` | 数组/文本 | 标签列表 | ["留学","悉尼"] |
| `upload_time` | 时间 | 发布时间 | "1706..." |
| `keyword_source` | 文本 | 来源关键词 | "澳洲留学" |
| `full_text` | 文本 | 完整文本 | "标题+正文+标签" |

### 评论表（Comments）

| 字段 | 类型 | 说明 |
|------|------|------|
| `comment_id` | 文本 | 评论唯一ID |
| `note_id` | 文本 | 所属笔记ID |
| `content` | 文本 | 评论内容 |
| `author_name` | 文本 | 评论者昵称 |
| `like_count` | 整数 | 点赞数 |

---

## 🎯 进阶配置

### 1. 安全爬取策略（推荐）

#### 按场景选择配置

**新账号建立信任期（第1-2周）**
```bash
python mimic_red_engine.py -k "测试关键词" -l 10 -d 50
```
- 每日上限：50条
- 持续时间：20-30分钟
- 目标：建立正常使用记录

**稳定运行期（第3周起）**
```bash
python mimic_red_engine.py -k "澳洲留学" -l 20 -d 100 -s sqlite
```
- 每日上限：100-150条（推荐）
- 持续时间：1-1.5小时
- 使用SQLite便于长期积累

**紧急数据需求（谨慎使用）**
```bash
python mimic_red_engine.py -k "关键词" -l 30 -d 200 --min-likes 100
```
- 每日上限：200条（接近极限）
- 需要：IP代理轮换 + 实时监控
- 风险：可能触发验证码

### 2. 时段选择建议

**推荐运行时段**（风控较松）：
- 工作日上午：09:00-11:00
- 工作日下午：15:00-17:00
- 深夜/凌晨：01:00-06:00（系统延迟自动×0.5）

**避开高峰时段**（风控严格）：
- 晚高峰：19:00-23:00（系统延迟自动×1.8）
- 午休时段：12:00-14:00
- 周末/节假日全天

### 3. 长期使用建议

```bash
# 每周至少休息1-2天，模拟真实用户

# 周一
python mimic_red_engine.py -k "关键词1" -d 100

# 周二  
python mimic_red_engine.py -k "关键词2" -d 100

# ...

# 周六日：完全停止爬取（休息）
```

### 4. IP代理配置（可选）

虽然当前版本未内置代理支持，但可通过以下方式配置：

**方案1：系统级代理**
```bash
# Mac/Linux
export http_proxy=http://127.0.0.1:7890
export https_proxy=http://127.0.0.1:7890

# Windows
set http_proxy=http://127.0.0.1:7890
set https_proxy=http://127.0.0.1:7890
```

**方案2：Chrome启动参数**

修改 `start_chrome.sh`：
```bash
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome \
    --remote-debugging-port=9222 \
    --proxy-server="http://your-proxy-ip:port" \
    --user-data-dir="$HOME/.chrome-debug-profile"
```

**代理选择建议**：
- ❌ **数据中心IP**（AWS/阿里云/腾讯云）：立即被标记
- ✅ **住宅IP**（家庭宽带）：成功率85-92%
- 💰 **成本**: 约¥300-500/月（5GB流量）
- 🔗 **推荐服务商**: 快代理、芝麻HTTP、WandouHTTP

### 5. 风险监控与应对

**立即停止爬取的4个信号**：

1. **连续出现404错误**（超过3次）
2. **触发验证码**
3. **搜索结果为空**（但手动搜索有结果）
4. **笔记详情页无法加载**

**应对措施**：

1. **停止爬取2-4小时**
2. **清除Cookie重新登录**（见下方账号管理章节）
3. **更换IP地址**（如使用代理）
4. **降低每日限额**（减少50%）

**恢复后的建议**：
```bash
# 从50条/天开始，观察3天
python mimic_red_engine.py -k "关键词" -l 10 -d 50

# 如无异常,逐步提升到100条/天
python mimic_red_engine.py -k "关键词" -l 20 -d 100
```

### 6. 法律合规提醒

| 使用场景 | 合法性 | 风险等级 | 说明 |
|---------|--------|---------|------|
| 个人学习/学术研究 | ✅ 合法 | 🟢 低 | 不超过10万条 |
| 商业数据分析（不盈利） | ⚠️ 灰色 | 🟡 中 | 需控制规模 |
| 数据转售/API服务 | ❌ 违法 | 🔴 极高 | 明确违法 |
| 累计爬取 > 100万条 | ❌ 刑事 | 🚫 刑事追诉 | 3-7年有期徒刑 |

**建议**：
- 仅用于个人学习
- 控制总量 < 10万条
- 不得爬取用户隐私（联系方式、精准位置）
- 不得用于商业盈利

---

## 🆘 常见问题排查

### 1. 无法连接浏览器

**错误信息**: `❌ 接管失败: Connection refused`

**原因**: Chrome未启动或未开启调试端口9222

**解决方法**:
```bash
# 1. 确保先启动 Chrome
./start_chrome.sh

# 2. 检查端口占用
lsof -i :9222  # Mac/Linux
netstat -ano | findstr :9222  # Windows

# 3. 如果端口被占用，杀死进程或重启电脑
```

### 2. 缺少依赖模块

**错误信息**: `ModuleNotFoundError: No module named 'xxx'`

**解决方法**:
```bash
# 推荐：使用锁定版本
pip install -r requirements-lock.txt

# 如果Excel格式报错
pip install pandas openpyxl

# 检查安装
pip list | grep DrissionPage
```

### 3. 数据提取失败（全是空）

**现象**: 日志显示 `⚠️ 未获取到正文`，但浏览器中可以看到内容

**原因**: 小红书网页结构更新，CSS选择器失效

**解决方法**:
1. 检查 `selectors.json` 文件
2. 在浏览器开发者工具（F12）中找到最新的CSS类名
3. 更新配置文件

### 4. 频繁触发反爬

**现象**: 连续出现 `🛑 检测到反爬限制` 或 `404`

**解决方法**:
1. **立即停止爬取**，休息2-4小时
2. **清除Cookie**重新登录（参考上方账号管理）
3. **降低频率**：减少每日总量50%
4. **更换IP**（如使用代理）
5. **更换账号**（如有多个）

### 5. 文件保存报错

**错误信息**: `Permission denied: xxx.xlsx`

**原因**: 文件正在被其他程序打开

**解决方法**: 关闭Excel/WPS等程序，然后重试

### 6. Excel格式乱码

**现象**: CSV文件在Excel中打开显示乱码

**解决方法**:

CSV文件已使用UTF-8-BOM编码，Excel应该能直接打开。如果仍有问题：

1. 使用Excel的"数据"-"从文本导入"功能
2. 选择UTF-8编码
3. 或使用Google Sheets打开（自动识别编码）

### 7. SQLite数据库无法打开

**解决方法**:

```bash
# 检查数据库是否损坏
sqlite3 sqlite_datas/notes.db "PRAGMA integrity_check;"

# 推荐的图形工具：
# Mac: DB Browser for SQLite (brew install --cask db-browser-for-sqlite)
# Windows: SQLiteStudio
# 在线: https://sqliteonline.com/
```

### 8. 账号切换与Cookie管理

**场景1：切换账号**

1. 在启动的 Chrome 窗口中，点击页面右上角的头像
2. 选择"退出登录"
3. 页面刷新后，使用新账号扫码登录

**场景2：清除 Cookie（解决登录异常）**

如果遇到登录死循环或状态异常：

1. 在 Chrome 地址栏左侧，点击 🔒（锁图标）
2. 点击 **"Cookie 和网站数据"**
3. 点击 **"管理 Cookie 和网站数据"**
4. 点击垃圾桶图标删除所有记录
5. 刷新页面重新登录

**场景3：彻底重置浏览器环境**

如果想完全重置浏览器环境：

```bash
# Mac/Linux
rm -rf ~/.chrome-debug-profile

# Windows
# 删除 %USERPROFILE%\.chrome-debug-profile 文件夹
```

然后重新运行 `start_chrome.sh` 并登录。

---

## 🔄 数据转换工具

### SQLite → CSV

```bash
# 导出笔记表
sqlite3 -header -csv sqlite_datas/notes.db "SELECT * FROM notes;" > notes.csv

# 导出评论表
sqlite3 -header -csv sqlite_datas/notes.db "SELECT * FROM comments;" > comments.csv
```

### CSV → Excel

```python
import pandas as pd

df = pd.read_csv('csv_datas/notes_xxx.csv')
df.to_excel('notes.xlsx', index=False)
```

### JSON → CSV

```python
import pandas as pd
import json

with open('json_datas/notes_xxx.json', 'r') as f:
    data = json.load(f)

pd.DataFrame(data['notes']).to_csv('notes.csv', index=False)
pd.DataFrame(data['comments']).to_csv('comments.csv', index=False)
```

### 合并多个CSV文件

```python
import pandas as pd
import glob

# 合并所有notes CSV
all_files = glob.glob('csv_datas/notes_*.csv')
df = pd.concat([pd.read_csv(f) for f in all_files], ignore_index=True)

# 去重
df = df.drop_duplicates(subset=['note_id'])

# 保存
df.to_csv('merged_notes.csv', index=False)
print(f"合并完成，共 {len(df)} 条笔记")
```

---

## 📞 获取帮助

如遇到其他问题，请：

1. 查看项目 [README.md](./README.md) 了解项目原理
2. 查看 [Issues](https://github.com/your-repo/issues) 中是否有类似问题
3. 提交新 Issue 时，请附上：
   - 错误日志（完整的终端输出）
   - 使用的命令
   - Python版本（`python --version`）
   - 操作系统版本

---

**最后更新**: 2026-02-12  
**版本**: v2.0（代码健康度优化版）
