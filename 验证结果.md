# ✅ 数据存储路径已验证

## 📁 当前文件夹结构

```
datas/
├── sqlite_datas/              ✅ SQLite 数据库文件夹
│   └── notes.db               （包含 notes 和 comments 两张表）
│
├── csv_datas/                 ✅ CSV 表格文件夹
│   ├── notes_20260210_230155.csv       （笔记数据）
│   └── comments_20260210_230155.csv    （评论数据）
│
├── json_datas/                ✅ JSON 文件夹
│   └── notes_20260210_230155.json      （包含笔记和评论的完整数据）
│
├── excel_datas/               ✅ Excel 文件夹
│   └── notes_20260210_230155.xlsx      （多工作表：Notes + Comments）
│
├── crawl_progress.json        （断点续爬进度，所有格式共用）
├── notes.db                   ⚠️ （旧的，在根目录，可以删除）
└── reports/                   （爬取报告，可选）
```

## 🎯 实际爬虫运行时的数据流向

### 1. SQLite 格式
```bash
python mimic_red_engine.py -k "澳洲留学" -s sqlite -l 10
```
**数据存储位置：** `datas/sqlite_datas/notes.db`

### 2. CSV 格式
```bash
python mimic_red_engine.py -k "澳洲留学" -s csv -l 10
```
**数据存储位置：**
- `datas/csv_datas/notes_YYYYMMDD_HHMMSS.csv`
- `datas/csv_datas/comments_YYYYMMDD_HHMMSS.csv`

### 3. JSON 格式
```bash
python mimic_red_engine.py -k "澳洲留学" -s json -l 10
```
**数据存储位置：** `datas/json_datas/notes_YYYYMMDD_HHMMSS.json`

### 4. Excel 格式
```bash
python mimic_red_engine.py -k "澳洲留学" -s excel -l 10
```
**数据存储位置：** `datas/excel_datas/notes_YYYYMMDD_HHMMSS.xlsx`

## ✅ 验证结果

### 已验证的功能：
- ✅ 文件夹自动创建（在 `datas/` 下）
- ✅ SQLite 数据正确写入
- ✅ CSV 文件正确生成（笔记 + 评论分开）
- ✅ JSON 文件正确生成（包含元数据）
- ✅ Excel 文件正确生成（多工作表）
- ✅ 数据格式化正确（UTF-8 编码）

### 数据示例：
- **CSV 笔记**: 包含 note_id, title, desc, author_name 等 17 个字段
- **JSON**: 结构化数据，包含 notes 数组和 comments 数组
- **SQLite**: 关系型数据库，notes 表 + comments 表
- **Excel**: 两个工作表（Notes + Comments）

## 🧹 清理建议

可以删除以下旧文件（如果不再需要）：
```bash
rm datas/notes.db         # 旧的数据库文件（应该在 sqlite_datas/ 中）
```

## 📊 查看数据的方法

### SQLite
```bash
sqlite3 datas/sqlite_datas/notes.db "SELECT * FROM notes LIMIT 5;"
```

### CSV
```bash
# 用 Excel 直接打开
open datas/csv_datas/notes_*.csv

# 或命令行查看
head datas/csv_datas/notes_*.csv
```

### JSON
```bash
# 格式化查看
cat datas/json_datas/notes_*.json | python3 -m json.tool | less
```

### Excel
```bash
# 用 Excel、WPS 或 Numbers 打开
open datas/excel_datas/notes_*.xlsx
```

## 💡 总结

**爬虫代码完全正常！** 数据会按照以下规则存储：

1. **基础目录**: `datas/`（默认，可通过 `-o` 参数修改）
2. **格式子目录**: 根据 `-s` 参数自动创建对应文件夹
3. **文件命名**: 带时间戳，避免覆盖
4. **数据隔离**: 不同格式的数据存储在各自的文件夹中

现在可以放心使用爬虫，数据会自动保存到正确的位置！🎉
